{
  "title": "Matterbridge Cozytouch Plugin",
  "description": "matterbridge-plugin-cozytouch v. 0.1.0",
  "type": "object",
  "properties": {
    "name": {
      "title": "Plugin Name",
      "description": "Plugin name",
      "type": "string",
      "readOnly": true,
      "ui:widget": "hidden"
    },
    "type": {
      "title": "Plugin Type",
      "description": "Plugin type",
      "type": "string",
      "readOnly": true,
      "ui:widget": "hidden"
    },
    "service": {
      "title": "Service",
      "description": "The Overkiz service to connect to. Use 'cozytouch' for Atlantic/Thermor/Sauter Cozytouch.",
      "type": "string",
      "default": "cozytouch",
      "enum": ["cozytouch", "tahoma", "connexoon", "somfy_europe", "somfy_australia", "somfy_north_america", "rexel", "hi_kumo", "flexom", "local"],
      "ui:widget": "select"
    },
    "user": {
      "title": "Username / Email",
      "description": "Your Cozytouch account email address.",
      "type": "string"
    },
    "password": {
      "title": "Password",
      "description": "Your Cozytouch account password.",
      "type": "string",
      "ui:widget": "password"
    },
    "pollingInterval": {
      "title": "Polling Interval (seconds)",
      "description": "How often to poll for device state updates (in seconds). Minimum 30 for cloud services.",
      "type": "number",
      "default": 60,
      "minimum": 10,
      "maximum": 300
    },
    "whiteList": {
      "title": "White List",
      "description": "Only the devices in the list will be exposed. If the list is empty, all the devices will be exposed.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true,
      "selectFrom": "name"
    },
    "blackList": {
      "title": "Black List",
      "description": "The devices in the list will not be exposed. If the list is empty, no devices will be excluded.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true,
      "selectFrom": "name"
    },
    "debug": {
      "title": "Enable Debug",
      "description": "Enable debug mode for the plugin. Only for debugging purposes, not recommended for production use.",
      "type": "boolean",
      "default": false
    },
    "unregisterOnShutdown": {
      "title": "Unregister On Shutdown",
      "description": "Unregister all devices on shutdown. Only for debugging purposes, not recommended for production use.",
      "type": "boolean",
      "default": false
    }
  },
  "required": ["user", "password"]
}
